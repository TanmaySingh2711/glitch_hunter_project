# ═══════════════════════════════════════════════════════════════════════
# FASTEST WAY (one command, no ordering to remember):
#     uv sync --active      (inside the activated venv_gpu; plain `uv sync` makes a separate .venv)
# That reads pyproject.toml, which already encodes the torch/CUDA rules
# explained below. If you'd rather use plain pip, follow the 2 steps.
#
# DO NOT run "pip install -r requirements.txt" by itself - it will not
# give you the GPU build of PyTorch. Follow the 2 steps below in order.
# See README.md "12. Installation" for the full copy-paste version of this.
# ═══════════════════════════════════════════════════════════════════════

# STEP 1 - install PyTorch first, separately (pick ONE line):
#   Have an NVIDIA GPU:  pip install torch==2.14.0 --index-url https://download.pytorch.org/whl/cu126
#   No NVIDIA GPU:       pip install torch==2.14.0
#   (torch alone: nothing here uses torchvision or torchaudio.)
#   (Why first: on Windows PyPI only has the CPU build, so a plain install
#   would never give you the GPU one. See README.md "12. Installation".)

# STEP 2 - install everything else:
#   pip install -r requirements.txt

stable-baselines3==2.9.0
gymnasium==1.3.0
cloudpickle==3.1.2
numpy==2.5.2
pygame==2.6.1
flask==3.1.3
flask-socketio==5.6.1
opencv-python==5.0.0.93
# Incident reports (reporting/render.py): PDF, and the GIF + embedded images.
fpdf2==2.8.8
pillow==12.3.0

# NOTE: eventlet was removed on purpose and must NOT be reinstalled.
# Flask-SocketIO picks its async mode from what's installed, so simply
# having eventlet in the venv would silently switch the server back to it.
# app.py now runs in threading mode - see the comment above the SocketIO()
# call there for why.

# OPTIONAL (developers) - the quality gates CI runs (see CONTRIBUTING.md):
#   pip install pytest==9.1.1 pytest-cov==7.1.0 ruff==0.16.6 mypy==2.3.1 pre-commit==4.6.2
#   python tools/check.py   # lint, types, fast tests, artifact hashes

# OPTIONAL - only if you want to see training graphs. Not needed to watch
# the AI play, and not needed to train either (training just skips the
# graphs if this isn't installed). Install it separately with:
#   pip install tensorboard
# then view the graphs with:
#   tensorboard --logdir ./logs/
