# Implementation Plan

_No plan currently active. The previous report (checkpoint-numbering fix,
migration abandonment, adaptive death memory) was fully implemented and has
been cleared — its conclusions now live as comments in the code they
describe._

Use this file to write up the next change before building it. Suggested
structure:

## Problem

What is actually wrong, with evidence (log excerpts, metric values, observed
behaviour). Separate the symptom from the cause.

## Options considered

What could be done, and why the rejected options were rejected.

## What was built

The chosen approach and where it lives in the code.

## Verification

How it was confirmed to work — not just that it runs, but that it fixed the
thing in the Problem section.
